# Signaleto VIP

اپلیکیشن پریمیوم سیگنال‌دهی خرید و فروش اسپات کریپتو

## ساختار پروژه

- `android/` → نسخه اندروید (Kotlin)
- `web/` → نسخه وب (PWA)
- `shared/` → کدهای مشترک (در صورت نیاز)
- `docs/` → مستندات

## نحوه اجرا

### نسخه وب
```bash
cd web
python -m http.server 8080
